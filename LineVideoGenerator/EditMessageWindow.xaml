<Window x:Class="LineVideoGenerator.EditMessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="編集" Height="200" Width="800" ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Grid.Column" Value="1"/>
            <Setter Property="Width" Value="75"/>
            <Setter Property="Height" Value="20"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="messageBox" Grid.Column="0" Margin="20" VerticalScrollBarVisibility="Auto" AcceptsReturn="True" TextWrapping="Wrap" TextChanged="MessageBox_TextChanged"/>
        <Button x:Name="changeButton" Content="変更" Margin="0 0 0 50" Click="ChangeButton_Click"/>
        <Button x:Name="cancelButton" Content="キャンセル" Margin="0 50 0 0" Click="CancelButton_Click"/>
    </Grid>
</Window>
